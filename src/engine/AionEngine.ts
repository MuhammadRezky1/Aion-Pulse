import { OpenAI } from \"openai\";`nexport class AionEngine { private client: OpenAI; constructor(apiKey: string) { this.client = new OpenAI({ apiKey }); } async analyze(content: string) { const response = await this.client.chat.completions.create({ model: \"gpt-4-turbo-preview\", messages: [{ role: \"system\", content: \"Analyze sentiment and extract topics as JSON.\" }, { role: \"user\", content }], response_format: { type: \"json_object\" } }); return response.choices[0].message.content; } }